services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: alpo
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d alpo"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
