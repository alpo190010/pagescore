---
phase: 01-pricing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/lib/analysis/types.ts
  - webapp/src/lib/analysis/conversion-model.ts
  - webapp/src/lib/analysis/helpers.ts
  - webapp/src/lib/analysis/__tests__/tier-gating.test.ts
  - webapp/src/lib/format.ts
  - webapp/src/app/admin/users/[id]/page.tsx
  - webapp/src/components/PaywallModal.tsx
  - webapp/src/app/analyze/page.tsx
autonomous: true
requirements:
  - PRICE-03

must_haves:
  truths:
    - "PlanTier type is 'free' | 'pro' with no mention of 'starter' or 'growth'"
    - "getDimensionAccess('free', anyKey) returns 'locked' for all 18 dimensions"
    - "getDimensionAccess('pro', anyKey) returns 'unlocked' for all 18 dimensions"
    - "STARTER_DIMENSIONS constant no longer exists anywhere in the codebase"
    - "No $29, $79, $149, 'starter', or 'growth' text appears in PaywallModal.tsx"
    - "Tier-gating tests pass with the new 2-tier model"
    - "TypeScript build succeeds with zero type errors"
  artifacts:
    - path: "webapp/src/lib/analysis/types.ts"
      provides: "PlanTier = 'free' | 'pro'"
      contains: "\"free\" | \"pro\""
    - path: "webapp/src/lib/analysis/conversion-model.ts"
      provides: "Simplified getDimensionAccess with 2-tier logic"
      exports: ["getDimensionAccess", "DimensionAccess", "DIMENSION_IMPACT_WEIGHTS"]
    - path: "webapp/src/lib/analysis/__tests__/tier-gating.test.ts"
      provides: "Rewritten tests for 2-tier model"
      contains: "describe(\"free plan\""
    - path: "webapp/src/components/PaywallModal.tsx"
      provides: "Simplified sign-up prompt modal without checkout tiers"
    - path: "webapp/src/app/analyze/page.tsx"
      provides: "Updated tier derivations and PaywallModal call sites"
  key_links:
    - from: "webapp/src/lib/analysis/types.ts"
      to: "webapp/src/lib/analysis/conversion-model.ts"
      via: "PlanTier import"
      pattern: "import type.*PlanTier.*from.*types"
    - from: "webapp/src/lib/analysis/helpers.ts"
      to: "webapp/src/lib/analysis/conversion-model.ts"
      via: "re-export of getDimensionAccess"
      pattern: "export.*getDimensionAccess.*from.*conversion-model"
    - from: "webapp/src/app/analyze/page.tsx"
      to: "webapp/src/components/PaywallModal.tsx"
      via: "dynamic import and props"
      pattern: "PaywallModal"
---

<objective>
Narrow PlanTier from 4 members to 2 and update every consumer across the codebase.

Purpose: Removes the stale starter/growth tier references that no longer match the product model (per D-08, D-11, PRICE-03). This is the type-system foundation that must land before the pricing page UI rewrite.

Output: All 8 consumer files updated atomically, tier-gating tests rewritten and green, TypeScript build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pricing-page/01-CONTEXT.md
@.planning/phases/01-pricing-page/01-RESEARCH.md
@.planning/phases/01-pricing-page/01-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From webapp/src/lib/analysis/types.ts (line 9) — CURRENT:
```typescript
export type PlanTier = "free" | "starter" | "growth" | "pro";
```

From webapp/src/lib/analysis/conversion-model.ts (lines 44-61) — CURRENT:
```typescript
export const STARTER_DIMENSIONS: ReadonlySet<string> = new Set([
  "socialProof", "images", "checkout", "title", "pricing", "shipping", "trust",
]);

export type DimensionAccess = "unlocked" | "locked";

export function getDimensionAccess(plan: PlanTier, dimensionKey: string): DimensionAccess {
  if (plan === "growth" || plan === "pro") return "unlocked";
  if (plan === "starter" && STARTER_DIMENSIONS.has(dimensionKey)) return "unlocked";
  return "locked";
}
```

From webapp/src/lib/analysis/helpers.ts (line 17) — CURRENT:
```typescript
export { calculateConversionLoss, calculateDollarLossPerThousand, getDimensionAccess, STARTER_DIMENSIONS } from "./conversion-model";
```

From webapp/src/lib/format.ts (lines 16-33) — CURRENT:
```typescript
export function planBadgeStyle(tier: string): React.CSSProperties {
  switch (tier) {
    case "pro":
      return { background: "var(--brand)", color: "var(--brand-light)" };
    case "growth":
      return { background: "var(--success)", color: "var(--on-primary)" };
    case "starter":
      return { background: "var(--surface-container-high)", color: "var(--text-primary)" };
    default:
      return { background: "var(--surface-container)", color: "var(--text-secondary)" };
  }
}
```

From webapp/src/app/admin/users/[id]/page.tsx (line 35) — CURRENT:
```typescript
const PLAN_TIERS = ["free", "starter", "growth", "pro"] as const;
```

From webapp/src/components/PaywallModal.tsx (lines 77-84) — CURRENT:
```typescript
interface PaywallModalProps {
  isOpen: boolean;
  onClose: () => void;
  userId: string;
  analyzedUrl: string;
  leakKey: string | null;
  userPlan?: PlanTier;
}
```

From webapp/src/app/analyze/page.tsx (lines 81-93) — CURRENT:
```typescript
const planTier: PlanTier = (planData?.plan as PlanTier) ?? "free";
const isShallow = planTier === "free";
const hasFullAccess = planTier === "growth" || planTier === "pro";
const isStarter = planTier === "starter";
const unlockedCount = hasFullAccess ? 18 : isStarter ? STARTER_DIMENSIONS.size : 0;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow PlanTier type, rewrite getDimensionAccess, and update all library consumers</name>
  <files>
    webapp/src/lib/analysis/types.ts,
    webapp/src/lib/analysis/conversion-model.ts,
    webapp/src/lib/analysis/helpers.ts,
    webapp/src/lib/format.ts,
    webapp/src/app/admin/users/[id]/page.tsx
  </files>
  <read_first>
    webapp/src/lib/analysis/types.ts,
    webapp/src/lib/analysis/conversion-model.ts,
    webapp/src/lib/analysis/helpers.ts,
    webapp/src/lib/format.ts,
    webapp/src/app/admin/users/[id]/page.tsx
  </read_first>
  <action>
Per D-11 and PRICE-03, narrow the PlanTier type and update all library-level consumers. Changes in exact order:

**1. webapp/src/lib/analysis/types.ts (line 9):**
Change:
```typescript
export type PlanTier = "free" | "starter" | "growth" | "pro";
```
To:
```typescript
export type PlanTier = "free" | "pro";
```
Update the JSDoc on line 8 from "Plan tier for feature gating (D076)" to "Plan tier for feature gating."

**2. webapp/src/lib/analysis/conversion-model.ts:**
- Delete lines 39-46 entirely: the `STARTER_DIMENSIONS` section header comment and the `STARTER_DIMENSIONS` constant + its JSDoc.
- Rewrite `getDimensionAccess` (currently lines 57-61) to:
```typescript
export function getDimensionAccess(plan: PlanTier, _dimensionKey: string): DimensionAccess {
  if (plan === "pro") return "unlocked";
  return "locked";
}
```
- Update the JSDoc above it (lines 51-56) to:
```typescript
/**
 * Resolve whether a dimension is accessible on the given plan.
 * - pro  -> all dimensions unlocked
 * - free -> all locked
 */
```
- The section header comment on line 39-41 ("Tier Gating -- starter-plan dimension access (D076)") should be updated to: "Tier Gating -- plan dimension access"

**3. webapp/src/lib/analysis/helpers.ts (line 17):**
Change:
```typescript
export { calculateConversionLoss, calculateDollarLossPerThousand, getDimensionAccess, STARTER_DIMENSIONS } from "./conversion-model";
```
To (remove STARTER_DIMENSIONS from the re-export):
```typescript
export { calculateConversionLoss, calculateDollarLossPerThousand, getDimensionAccess } from "./conversion-model";
```

**4. webapp/src/lib/format.ts (lines 16-33):**
Remove the `case "growth":` block (lines 20-21) and the `case "starter":` block (lines 22-25). Result:
```typescript
export function planBadgeStyle(tier: string): React.CSSProperties {
  switch (tier) {
    case "pro":
      return { background: "var(--brand)", color: "var(--brand-light)" };
    default:
      return { background: "var(--surface-container)", color: "var(--text-secondary)" };
  }
}
```

**5. webapp/src/app/admin/users/[id]/page.tsx (line 35):**
Change:
```typescript
const PLAN_TIERS = ["free", "starter", "growth", "pro"] as const;
```
To:
```typescript
const PLAN_TIERS = ["free", "pro"] as const;
```
  </action>
  <verify>
    <automated>cd /Users/aleksandrephatsatsia/projects/alpo/webapp && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - types.ts line 9 contains `"free" | "pro"` and does NOT contain `"starter"` or `"growth"`
    - conversion-model.ts does NOT contain `STARTER_DIMENSIONS`
    - conversion-model.ts getDimensionAccess body is `if (plan === "pro") return "unlocked"; return "locked";`
    - helpers.ts line 17 does NOT contain `STARTER_DIMENSIONS`
    - format.ts planBadgeStyle does NOT contain `case "growth"` or `case "starter"`
    - admin/users/[id]/page.tsx PLAN_TIERS contains exactly `["free", "pro"]`
  </acceptance_criteria>
  <done>PlanTier is "free" | "pro" everywhere. STARTER_DIMENSIONS deleted. getDimensionAccess uses clean 2-tier logic. format.ts and admin dropdown updated. No starter/growth references remain in library code.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify PaywallModal and update analyze/page.tsx consumers</name>
  <files>
    webapp/src/components/PaywallModal.tsx,
    webapp/src/app/analyze/page.tsx
  </files>
  <read_first>
    webapp/src/components/PaywallModal.tsx,
    webapp/src/app/analyze/page.tsx
  </read_first>
  <action>
Per D-10 and PRICE-03, gut PaywallModal of all LemonSqueezy checkout logic and old tier pricing, then update analyze/page.tsx to match the simplified props interface and remove starter-tier UI branches.

**1. webapp/src/components/PaywallModal.tsx — full rewrite:**

Keep the "use client" directive, Modal shell, close button, and header. Remove everything related to LemonSqueezy checkout and subscription tier listing.

Delete entirely:
- Lines 23-26: `LS_STORE_URL`, `LS_VARIANT_STARTER`, `LS_VARIANT_GROWTH`, `LS_VARIANT_PRO` env var declarations
- Lines 28-66: `SubscriptionTier` interface and `TIERS` array ($29/$79/$149 data)
- Lines 68-75: `buildCheckoutUrl` function
- Line 94: `const [checkoutClicked, setCheckoutClicked] = useState(false);` state
- Line 96: `const isStarter = userPlan === "starter";` derivation
- Lines 142-244: The entire `TIERS.map(...)` subscription tier list JSX
- Line 247-249: "Secure checkout via LemonSqueezy" footer text

Simplify the props interface to:
```typescript
interface PaywallModalProps {
  isOpen: boolean;
  onClose: () => void;
}
```

Remove unused imports: `CrownIcon`, `RocketLaunchIcon`, `BuildingsIcon`, `CheckCircleIcon`, `captureEvent`, `PlanTier`. Keep: `XIcon`, `LockKeyIcon`, `useState` (remove if no state remains), `Button`, `Modal`, `ModalTitle`, `ModalClose`.

Update the component signature to:
```typescript
export default function PaywallModal({ isOpen, onClose }: PaywallModalProps) {
```

Update the header content (replace the tier-aware branching):
- Title: `"Sign up to get full access"` (no isStarter branch)
- Description: `"Get detailed fixes, actionable recommendations, and step-by-step guides to boost your conversion rate."` (no isStarter branch)
- ariaLabel: `"Sign up to unlock full report"`

Keep the Modal shell pattern intact (open/close, gradient bar, close button, header icon). After the header description, add a placeholder comment for Phase 2:
```tsx
{/* Phase 2 will add sign-up / paywall gate CTA here */}
```

**2. webapp/src/app/analyze/page.tsx — update imports and tier derivations:**

In the import block (line 31), remove `STARTER_DIMENSIONS` from the destructured import:
```typescript
// BEFORE:
  getDimensionAccess,
  STARTER_DIMENSIONS,
// AFTER:
  getDimensionAccess,
```

Update derived variables (lines 84-93). Replace:
```typescript
const isShallow = planTier === "free";
const hasFullAccess = planTier === "growth" || planTier === "pro";
const isStarter = planTier === "starter";
const unlockedCount = hasFullAccess ? 18 : isStarter ? STARTER_DIMENSIONS.size : 0;
```
With:
```typescript
const isShallow = planTier === "free";
const hasFullAccess = planTier === "pro";
```
Remove `isStarter` and `unlockedCount` entirely -- they are no longer needed.

Update both PaywallModal call sites (around lines 289-296 and lines 521-528). Remove the extra props that no longer exist on the simplified interface. Change both from:
```tsx
<PaywallModal
  isOpen={paywallOpen}
  onClose={closePaywall}
  userId={planData?.userId ?? ""}
  analyzedUrl={url}
  leakKey={paywallLeakKey}
  userPlan={planTier}
/>
```
To:
```tsx
<PaywallModal
  isOpen={paywallOpen}
  onClose={closePaywall}
/>
```

Update the issues header text (around line 354-359). Replace the 3-way ternary:
```tsx
{isShallow
  ? `${leaks.length} conversion leaks identified. Subscribe to see detailed fixes.`
  : isStarter
    ? `${leaks.length} conversion leaks identified. ${unlockedCount} dimensions unlocked — upgrade to access all ${leaks.length}.`
    : `${leaks.length} conversion leaks identified. Click any to see the details.`
}
```
With a 2-way ternary (isShallow or full access):
```tsx
{isShallow
  ? `${leaks.length} conversion leaks identified. Sign up to see detailed fixes.`
  : `${leaks.length} conversion leaks identified. Click any to see the details.`
}
```

Update the CTA card condition (around line 381). Replace `!hasFullAccess` with the updated derivation. Since `hasFullAccess` is now `planTier === "pro"`, the condition `(isTeaser || !hasFullAccess)` is already correct -- keep it.

Update CTACard label/buttonLabel props (around lines 388-398). Remove the isStarter branches:
```tsx
label={
  isTeaser ? undefined
  : isShallow ? "Get All Fixes"
  : undefined
}
buttonLabel={
  isTeaser ? undefined
  : isShallow ? "Subscribe Now"
  : undefined
}
```

Delete the entire "Starter tier: upgrade CTA" section (around lines 483-515) -- the entire JSX block `{result && showLeaks && !isTeaser && isStarter && (...)}`. This is the "Unlock All Dimensions" / "Upgrade to Growth" section that no longer applies.

Update the free-tier CTA section text (around line 456). Change "Subscribe to see detailed fixes" to "Sign up to see detailed fixes" since there is no subscription anymore.
  </action>
  <verify>
    <automated>cd /Users/aleksandrephatsatsia/projects/alpo/webapp && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - PaywallModal.tsx does NOT contain `LS_STORE_URL`, `LS_VARIANT`, `buildCheckoutUrl`, `SubscriptionTier`, `TIERS`, `$29`, `$79`, `$149`, `isStarter`, `checkoutClicked`, `LemonSqueezy`, `captureEvent`
    - PaywallModal.tsx props interface has exactly `isOpen: boolean` and `onClose: () => void` (2 props only)
    - PaywallModal.tsx contains `"Sign up to get full access"` as modal title
    - analyze/page.tsx does NOT contain `STARTER_DIMENSIONS`, `isStarter`, `unlockedCount`, `"Upgrade to Growth"`, `"Unlock All Dimensions"`
    - analyze/page.tsx `hasFullAccess` is `planTier === "pro"` (not `planTier === "growth" || planTier === "pro"`)
    - analyze/page.tsx PaywallModal call sites have exactly 2 props: `isOpen` and `onClose`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>PaywallModal stripped of all checkout logic and old tier references, kept as minimal modal shell for Phase 2. analyze/page.tsx updated with 2-tier derivations, starter UI branches removed, PaywallModal props simplified. Build is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite tier-gating tests for 2-tier model</name>
  <files>webapp/src/lib/analysis/__tests__/tier-gating.test.ts</files>
  <read_first>
    webapp/src/lib/analysis/__tests__/tier-gating.test.ts,
    webapp/src/lib/analysis/conversion-model.ts,
    webapp/src/lib/analysis/constants.tsx
  </read_first>
  <behavior>
    - Test 1: getDimensionAccess("free", key) returns "locked" for all 18 ACTIVE_DIMENSIONS
    - Test 2: getDimensionAccess("pro", key) returns "unlocked" for all 18 ACTIVE_DIMENSIONS
    - Test 3: getDimensionAccess("free", "nonexistent") returns "locked"
    - Test 4: getDimensionAccess("pro", "nonexistent") returns "unlocked"
  </behavior>
  <action>
Rewrite the entire test file. The STARTER_DIMENSIONS import and all starter/growth test blocks must be deleted. The new file should be:

```typescript
import { describe, it, expect } from "vitest";
import { getDimensionAccess } from "../conversion-model";
import { ACTIVE_DIMENSIONS } from "../constants";
import type { PlanTier } from "../types";
import type { DimensionAccess } from "../conversion-model";

/* ══════════════════════════════════════════════════════════════
   Tier-Gating Tests — 2-tier model (free / pro)
   ══════════════════════════════════════════════════════════════ */

const ALL_DIMENSION_KEYS = [...ACTIVE_DIMENSIONS];

describe("getDimensionAccess", () => {
  describe("free plan", () => {
    it("returns locked for all 18 active dimensions", () => {
      for (const key of ALL_DIMENSION_KEYS) {
        expect(getDimensionAccess("free", key)).toBe("locked" satisfies DimensionAccess);
      }
    });
  });

  describe("pro plan", () => {
    it("returns unlocked for all 18 active dimensions", () => {
      for (const key of ALL_DIMENSION_KEYS) {
        expect(getDimensionAccess("pro", key)).toBe("unlocked" satisfies DimensionAccess);
      }
    });
  });

  describe("unknown dimension key", () => {
    it("returns locked for free plan", () => {
      expect(getDimensionAccess("free", "nonexistent")).toBe("locked");
    });

    it("returns unlocked for pro plan (all dims unlocked)", () => {
      expect(getDimensionAccess("pro", "nonexistent")).toBe("unlocked");
    });
  });
});
```

Key differences from the old test file:
- No `STARTER_DIMENSIONS` import (constant deleted in Task 1)
- No `EXPECTED_STARTER_KEYS` constant
- No `describe("STARTER_DIMENSIONS", ...)` block
- No `describe("starter plan", ...)` block
- No `describe("growth plan", ...)` block
- No `starter`/`growth` test cases in "unknown dimension key" block
- `PlanTier` import kept for type-level documentation even though not directly used in assertions
  </action>
  <verify>
    <automated>cd /Users/aleksandrephatsatsia/projects/alpo/webapp && npx vitest run src/lib/analysis/__tests__/tier-gating.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tier-gating.test.ts does NOT contain `STARTER_DIMENSIONS`, `starter`, `growth`, `EXPECTED_STARTER_KEYS`
    - tier-gating.test.ts contains exactly 2 describe blocks for plans: `"free plan"` and `"pro plan"`
    - tier-gating.test.ts contains `"unknown dimension key"` describe block with 2 test cases
    - `npx vitest run src/lib/analysis/__tests__/tier-gating.test.ts` exits with 0 and reports 4 tests passed
  </acceptance_criteria>
  <done>All tier-gating tests rewritten for 2-tier model. 4 tests pass: free locked for all dims, pro unlocked for all dims, free locked for unknown key, pro unlocked for unknown key. No references to starter or growth remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> session | useSession() reads existing next-auth cookie; no new auth surface |
| analyze/page.tsx -> PaywallModal | PaywallModal previously passed userId/URL to LemonSqueezy checkout; now removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | getDimensionAccess after type change | mitigate | New logic is strictly MORE restrictive: free=locked, pro=unlocked. No accidental over-permissiveness. Verified by tier-gating.test.ts asserting "free" returns "locked" for all 18 dims. |
| T-01-02 | Information Disclosure | PaywallModal leaking LemonSqueezy env vars | mitigate | All LS_STORE_URL, LS_VARIANT_* declarations removed from PaywallModal.tsx entirely. No env vars remain in client bundle from this component. |
| T-01-03 | Tampering | PlanTier type cast in analyze/page.tsx | accept | `(planData?.plan as PlanTier)` casts server response to PlanTier. If server sends "starter", TypeScript narrows it but runtime behavior falls through to default ("locked"). This is safe -- getDimensionAccess returns "locked" for any non-"pro" value. |
</threat_model>

<verification>
1. `cd webapp && npx tsc --noEmit` -- TypeScript build clean (zero errors)
2. `cd webapp && npx vitest run src/lib/analysis/__tests__/tier-gating.test.ts` -- 4 tests pass
3. `cd webapp && npm run test` -- full test suite green
4. grep -r "starter" webapp/src/lib/analysis/ -- returns zero matches (no starter references in analysis lib)
5. grep -r "STARTER_DIMENSIONS" webapp/src/ -- returns zero matches
6. grep -rn "growth" webapp/src/lib/format.ts webapp/src/app/admin/users/ -- returns zero matches
7. grep -n "LS_STORE_URL\|LS_VARIANT\|LemonSqueezy\|buildCheckoutUrl" webapp/src/components/PaywallModal.tsx -- returns zero matches
</verification>

<success_criteria>
- PlanTier = "free" | "pro" across entire codebase (PRICE-03, D-11)
- STARTER_DIMENSIONS deleted from conversion-model.ts and helpers.ts re-export (D-11)
- getDimensionAccess returns locked for free, unlocked for pro (D-11)
- PaywallModal gutted of LemonSqueezy checkout, $29/$79/$149 pricing, starter/growth branches (D-09, D-10)
- analyze/page.tsx uses 2-tier derivation, no isStarter/unlockedCount, simplified PaywallModal props (D-11)
- format.ts and admin dropdown use 2-tier values only (D-11)
- All tests pass, TypeScript build clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-pricing-page/01-01-SUMMARY.md`
</output>
