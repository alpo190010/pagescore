---
phase: 02-paywall-gates
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false
requirements: [GATE-01, GATE-02, GATE-03]
must_haves:
  truths:
    - "Anonymous user who runs a scan sees dimension scores but not recommendation text"
    - "Signed-in user sees all 18 dimensions with full recommendations"
    - "Results page displays a signup prompt nudging anonymous users to create an account"
  artifacts: []
  key_links: []
---

<objective>
Visually verify the full paywall gate flow end-to-end: anonymous scan, locked cards, inline CTA, AuthModal signup, and post-signup unlock.

Purpose: The locked card redesign, anonymous scan flow, and signup-triggered unlock involve visual and interaction states that cannot be fully validated by automated tests alone. This checkpoint confirms the implementation matches the UI-SPEC contract.

Output: Human-verified approval of the paywall gate flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-paywall-gates/02-UI-SPEC.md
@.planning/phases/02-paywall-gates/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification: Paywall gate flow</name>
  <files>webapp/src/app/analyze/page.tsx</files>
  <what-built>
Full paywall gate implementation across the results page:
- Anonymous users trigger real backend scans (not fake SAMPLE_SCAN data)
- 18 IssueCards show as locked: dimension name + score + impact badge + lock icon + "Sign up to see fixes"
- Inline CTACard appears as last item in issues grid: "Your page has {N} issues. Sign up free to see how to fix them."
- Clicking any locked card or CTACard opens AuthModal in signup mode
- After signup, useSession re-fires, page unlocks all cards with full recommendations
- ScoreRing and PluginCTACard (revenue loss) are fully visible for anonymous users
- Bottom gradient banner removed for anonymous users; minimal "Analyze Another Page" button shown instead
- Credit exhaustion screen shows "Join Pro Waitlist" linking to /pricing
- PaywallModal deleted entirely
  </what-built>
  <action>No code changes. Human visually verifies the paywall gate flow described in how-to-verify. This is a checkpoint:human-verify task.</action>
  <how-to-verify>
**Prerequisites:**
Start both the backend API and frontend dev servers.

**Flow 1: Anonymous scan (GATE-01)**
1. Open an incognito/private browser window (ensures no session)
2. Visit `http://localhost:3000`
3. Enter a real Shopify product URL and click Analyze
4. Wait for scan to complete (should take 15-30 seconds -- real Playwright scan, not instant)
5. VERIFY: ScoreRing shows with a real score (not the Gymshark sample score of 52)
6. VERIFY: Revenue loss estimate (PluginCTACard) is visible
7. VERIFY: All 18 IssueCards show as collapsed locked cards with:
   - Category icon + score number on top row
   - Dimension name (e.g., "Social Proof", "Structured Data")
   - Impact badge (HIGH/MED/LOW) with color coding
   - Lock icon + "Sign up to see fixes" at bottom
   - NO recommendation text, NO problem description, NO signal checklists
8. VERIFY: The last item in the issues grid is a dark CTACard reading "Your page has {N} issues. Sign up free to see how to fix them." with a "Create Free Account" button
9. VERIFY: No gradient banner at the bottom of the page
10. VERIFY: An "Analyze Another Page" button appears below the grid

**Flow 2: Locked card interaction (GATE-01, GATE-03)**
11. Click any locked IssueCard
12. VERIFY: AuthModal opens with "Sign Up" heading (not "Sign In")
13. Close the modal
14. Click the "Create Free Account" button on the inline CTACard
15. VERIFY: AuthModal opens again in signup mode

**Flow 3: Authenticated access (GATE-02)**
16. Sign up or sign in via the AuthModal (use Google OAuth or email/password)
17. VERIFY: After authentication, the page refreshes/re-renders
18. VERIFY: All 18 IssueCards are now expandable (click to expand)
19. VERIFY: Expanded cards show full recommendation text and signal checklists
20. VERIFY: The inline CTACard is no longer visible
21. VERIFY: The "Scan Another" section appears for authenticated users

**Flow 4: Rate limit (if easy to test)**
22. In the incognito window (anonymous), try running 4 scans
23. VERIFY: The 4th scan should return a rate limit error (429)

**Flow 5: No PaywallModal references**
24. Ctrl+F in the browser for "Upgrade Plan" -- should not appear anywhere
25. VERIFY: No paywall-related modals appear during any flow
  </how-to-verify>
  <verify>Human approval of all 5 verification flows</verify>
  <done>All 3 GATE requirements confirmed working in the browser: anonymous locked cards, authenticated full access, signup prompt visible</done>
  <resume-signal>Type "approved" if all flows work correctly, or describe specific issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan -- no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No code changes in this plan -- threats covered by Plans 01 and 02 |
</threat_model>

<verification>
Human visual verification of all three GATE requirements:
- GATE-01: Anonymous user sees dimension scores but not recommendations
- GATE-02: Signed-in user sees all 18 dimensions with full recommendations
- GATE-03: Results page shows signup prompt nudging anonymous users
</verification>

<success_criteria>
- Human approves all 5 verification flows
- All 3 GATE requirements confirmed working in the browser
</success_criteria>

<output>
After completion, create `.planning/phases/02-paywall-gates/02-03-SUMMARY.md`
</output>
